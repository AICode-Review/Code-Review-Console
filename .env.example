# Same Supabase project as frontend/ — the console reuses the same GitHub/Bitbucket
# sign-in; access is then gated server-side by users.is_platform_admin (see
# backend/.env.example's ADMIN_BOOTSTRAP_EMAILS to grant the first admin).
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://localhost:4000
